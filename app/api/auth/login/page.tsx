import "./login.css";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Login"
};

const LoginPage = () => {
  return (
    <h2>Login Page</h2>
  );
};

export default LoginPage;